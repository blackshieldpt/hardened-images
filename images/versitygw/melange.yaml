# Builds versitygw from source (versitygw isn't in Wolfi). Pure-Go, CGO disabled,
# so the result is a static binary. v1.6.0 already carries current x/crypto
# (v0.53.0) and x/net (v0.56.0), but its x/text v0.38.0 is affected by GO-2026-5970,
# so that one is bumped below.
package:
  name: versitygw-hardened
  version: 1.7.0
  epoch: 0
  description: "Versity S3 Gateway (built from source)"
  copyright:
    - license: Apache-2.0

environment:
  contents:
    repositories:
      - https://packages.wolfi.dev/os
    keyring:
      - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
    packages:
      - busybox
      - ca-certificates-bundle
      - go
      - git

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/versity/versitygw
      tag: v${{package.version}}
      expected-commit: 8a3dbcf97d996ea4955426e6b8a76eb96d2cd92a
  - name: Bump vulnerable Go dependencies
    runs: |
      go get golang.org/x/text@v0.39.0
      go mod tidy
  - name: Compile static binary
    runs: |
      CGO_ENABLED=0 go build \
        -ldflags "-s -w -X=main.Version=v${{package.version}}" \
        -o "${{targets.destdir}}/usr/bin/versitygw" ./cmd/versitygw
  - runs: |
      "${{targets.destdir}}/usr/bin/versitygw" --version

update:
  enabled: true
  github:
    identifier: versity/versitygw
    use-tag: true
    tag-filter: "v"
    strip-prefix: "v"
